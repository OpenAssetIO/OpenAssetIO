# yaml-language-server: $schema=../../python/openassetio_codegen/schema.json
# yamllint disable-line rule:document-start
package: openassetio-codegen-test-specifications-only

description: >
    Test classes to validate the integrity of the openassetio-codegen tool when only specifications
    are defined.

specifications:
    test:
        description: More test specifications.
        members:
            Some:
                description: Some specification
                traitSet:
                    - package: openassetio-codegen-test-all
                      namespace: aNamespace
                      name: AllProperties
                    - package: openassetio-codegen-test-traits-only
                      namespace: test
                      name: Another
